apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ai-platform.fullname" . }}
  labels:
    {{- include "ai-platform.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "ai-platform.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        {{- include "ai-platform.labels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ default (include "ai-platform.fullname" .) .Values.serviceAccount.name }}
      containers:
        - name: api
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: PLATFORM_DB_PATH
              value: /data/registry.db
            - name: PLATFORM_ENV
              value: {{ .Values.env.PLATFORM_ENV | quote }}
            - name: PLATFORM_DEFAULT_NAMESPACE
              value: {{ .Values.env.PLATFORM_DEFAULT_NAMESPACE | quote }}
            - name: PLATFORM_PRIMARY_REGION
              value: {{ .Values.env.PLATFORM_PRIMARY_REGION | quote }}
            - name: PLATFORM_ALLOW_DEV_LOGIN
              value: {{ .Values.env.PLATFORM_ALLOW_DEV_LOGIN | quote }}
            - name: PLATFORM_AUTH_REQUIRED
              value: {{ .Values.env.PLATFORM_AUTH_REQUIRED | quote }}
            - name: PLATFORM_GOVERNOR_BACKEND
              value: {{ .Values.env.PLATFORM_GOVERNOR_BACKEND | quote }}
            - name: PLATFORM_REDIS_URL
              value: {{ .Values.env.PLATFORM_REDIS_URL | quote }}
            - name: PLATFORM_OIDC_ISSUER
              value: {{ .Values.env.PLATFORM_OIDC_ISSUER | quote }}
            - name: PLATFORM_OIDC_CLIENT_ID
              value: {{ .Values.env.PLATFORM_OIDC_CLIENT_ID | quote }}
            - name: PLATFORM_OIDC_REDIRECT_URI
              value: {{ .Values.env.PLATFORM_OIDC_REDIRECT_URI | quote }}
            - name: PLATFORM_SECRETS_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "ai-platform.fullname" . }}-secrets
                  key: secrets-key
            - name: PLATFORM_AUTH_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "ai-platform.fullname" . }}-secrets
                  key: auth-secret
            - name: PLATFORM_OIDC_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "ai-platform.fullname" . }}-secrets
                  key: oidc-client-secret
                  optional: true
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "ai-platform.fullname" . }}-secrets
                  key: openai-api-key
                  optional: true
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "ai-platform.fullname" . }}-secrets
                  key: anthropic-api-key
                  optional: true
            - name: BEDROCK_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "ai-platform.fullname" . }}-secrets
                  key: bedrock-api-key
                  optional: true
          volumeMounts:
            - name: data
              mountPath: /data
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        - name: data
          {{- if .Values.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "ai-platform.fullname" . }}-data
          {{- else }}
          emptyDir: {}
          {{- end }}
